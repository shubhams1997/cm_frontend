import React from "react";
import Base from "../core/Base";

function Product(props) {
  return (
    <Base title="Products" description="Manage your Products here."></Base>
  );
}

export default Product;
